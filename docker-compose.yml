version: "3.9"

services:
  postgres:
    image: postgres:17
    container_name: running-db
    environment:
      POSTGRES_DB: running
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
